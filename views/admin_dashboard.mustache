<!DOCTYPE html>
<html lang="en">
<head>
    <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" integrity="sha384-<correct-integrity-value>" crossorigin="anonymous">
    <!-- Add this line to include Font Awesome JavaScript library -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/js/all.min.js" integrity="sha384-<correct-integrity-value>" crossorigin="anonymous"></script>

    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="/css/student.css">

</head>
<body>

<header>
    <h1>Admin Dashboard</h1>
        <a href="/" class="logout-button">Logout</a>

</header>

<main>
    
<div class="user-profile">
    <i class="fas fa-user-circle" font-size: 140px;></i>
    <div class="user-details">
        <div class="user-details-column">
        <div>
            <p>Welcome, {{ userData.username }}</p> <br>
            <p>Your role: {{ userData.role }}</p>
            <p>First name: {{ userData.firstName }}</p>
            <p>Last name: {{ userData.lastName }}</p>
        </div>
        </div>
    </div>
</div>

</main>
 <div class="header-buttons">
            <a href="/displayStudent/{{userData.userId}}" class="dashboard-button">Manage Student</a>
            <a href="/manageMentor" class="dashboard-button">Mange Mentor</a>
        </div>


</body>
</html>
